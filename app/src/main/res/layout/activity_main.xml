<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".example.MainActivity">

    <com.github.heesung6701.timepicker.timepicker.TimePickerView
        android:id="@+id/time_picker"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:clockSize="24"
        app:clockPadding="50"
        app:timePadding="60"
        app:timeTextColor="#000000"
        app:timeSelectedTextColor="#ffffff"
        app:timeBackgroundColor="#000000"
        app:timeTextSize="16px"
        app:clockBorderColor="#000000"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/tv">

        <EditText
            android:id="@+id/edt"
            android:layout_width="50dp"
            android:layout_height="wrap_content"
            />

        <Button
            android:id="@+id/btn"
            android:layout_width="50dp"
            android:layout_height="wrap_content"
            android:text="클릭"
            />
    </LinearLayout>


    <TextView
        android:id="@+id/tv"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:textSize="18sp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="test Text"
        />
</androidx.constraintlayout.widget.ConstraintLayout>